{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start",
            "command": "docker",
            "args": ["compose", "-f", "docker-compose/docker-compose.yml", "up", "--attach", "consumerApi"]
        }
    ]
}

import 'package:flutter/material.dart';

class IdentityDetails extends StatelessWidget {
  const IdentityDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('I am at identity details.');
  }
}

import 'package:flutter/material.dart';

class TiersOverview extends StatelessWidget {
  const TiersOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('I am at tiers overview');
  }
}
